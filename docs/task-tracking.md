# 开发计划与任务跟踪

## 1. 文档约定

本文件用于指导短期和中期开发，不只是记录想法。

维护规则：

- 开发开始前，先把任务写入本文件
- 开发完成后，及时更新本文件中的状态
- 范围变化时，及时调整优先级和任务拆分
- 已完成工作必须保留记录，不直接删除
- 已完成工作按树结构标记，便于追踪父任务和子任务的完成情况

状态约定：

- `[ ]` 未开始
- `[-]` 进行中
- `[x]` 已完成

优先级约定：

- `P0` 当前阻塞项，优先处理
- `P1` 当前阶段关键项
- `P2` 可顺延项

## 2. 当前阶段

项目状态：工程骨架已完成，进入第一阶段开发准备

当前目标：

- 把文档变成可执行的开发指引
- 明确短期任务和中期任务
- 明确每个任务的完成结果

## 3. Now

当前 1 到 2 周内优先处理的事项。

- [-] `P1` Codex thread 助理接入
  - [x] 明确一次性助理邀请 URL 规则
  - [x] 明确 `backend_thread_id` 绑定规则
  - [x] 明确 Codex skill 作为推荐加入入口
  - [x] 设计助理邀请数据模型
  - [x] 设计一次性助理邀请接口
  - [ ] 实现 `codex_cli` thread 绑定 adapter
  - [ ] 定义加入聊天室的 Codex skill

- [x] `P2` 本地 Agent 接入
  - [x] 定义统一 `AgentAdapter` 接口
  - [x] 实现本地子进程 adapter
  - [x] 接入首个本地 Agent 运行链路
  - [x] stdout 增量转流式事件
  - [x] 最终结果落消息

- [x] `P2` 前端演示壳
  - [x] 房间页面
  - [x] 成员列表
  - [x] 消息列表
  - [x] 流式输出渲染
  - [x] 审批交互区

- [x] `P1` 收紧 Agent 执行与管理边界
  - [x] 添加 Agent 绑定 `wsToken`
  - [x] 本地子进程支持超时回收
  - [x] 成员公开数据不暴露 adapter 配置

- [x] `P1` 拆分公开 DTO 与内部模型
  - [x] 新增公开成员 DTO
  - [x] 成员查询与事件改用公开 DTO
  - [x] 前端不再依赖内部成员模型

- [x] `P1` 扩展公开 DTO 覆盖范围
  - [x] 新增公开消息 DTO
  - [x] 新增公开审批 DTO
  - [x] 消息与审批接口改用公开 DTO
  - [x] 公开映射抽到独立模块

- [x] `P1` 收紧身份与命名约束
  - [x] 收紧 `display_name` 规则，避免与 mention 解析冲突
  - [x] 发消息绑定 `wsToken`
  - [x] 审批操作绑定 `wsToken`

- [x] `P0` 更新业务与接口文档中的关键缺口
  - [x] 明确房间内 `display_name` 唯一规则
  - [x] 明确助理成员的显示规则
  - [x] 明确 `wsToken` 的生命周期和重连规则

- [x] `P0` 定义第一版领域模型
  - [x] 定义 `Room`
  - [x] 定义 `Member`
  - [x] 定义 `Message`
  - [x] 定义 `Mention`
  - [x] 定义 `AgentSession`

- [x] `P0` 定义第一版实时事件协议
  - [x] 定义房间事件
  - [x] 定义消息事件
  - [x] 定义审批事件
  - [x] 定义 Agent 会话事件

- [x] `P1` 定义第一版 SQLite 表结构
  - [x] 建立基础表
  - [x] 建立必要索引
  - [x] 约定迁移方式

- [x] `P1` 打通本地开发启动链路
  - [x] 后端可启动
  - [x] 前端可启动
  - [x] 前后端可同时启动
  - [x] 补充启动说明

## 4. Next

紧接着进入的开发任务。

- [x] `P1` 房间聊天 MVP
  - [x] 创建房间
  - [x] 链接邀请加入房间
  - [x] 昵称进入房间
  - [x] 建立 WebSocket 连接
  - [x] 广播成员加入/离开事件
  - [x] 发送消息
  - [x] 广播消息事件
  - [x] 持久化基础消息记录

- [x] `P1` Agent 作为成员
  - [x] 添加独立 Agent 成员
  - [x] 添加助理 Agent 成员
  - [x] 建立 owner 关联关系
  - [x] `@成员` 解析与匹配
  - [x] 独立 Agent 触发流程

- [x] `P1` 助理审批链路
  - [x] 创建审批请求
  - [x] owner 在线状态判定
  - [x] 同意调用
  - [x] 拒绝调用
  - [x] 审批结果广播
  - [x] 超时与失败处理

## 5. Later

中期任务，按开发进展推进。

- [ ] `P2` 面向酒馆式像素 UI 的表现层预留
  - [ ] 整理事件消费边界
  - [ ] 避免 Web UI 反向绑定后端模型
  - [ ] 为未来替换 UI 保持协议稳定

## 6. 已完成

- [x] `P0` 文档与工程基线初始化
  - [x] 建立基础文档目录
  - [x] 完成 README
  - [x] 完成业务设计文档
  - [x] 完成技术基线文档
  - [x] 完成接口文档
  - [x] 补充开发计划维护约定
  - [x] 补充关键业务与连接规则
  - [x] 定义第一版领域模型
  - [x] 定义第一版实时事件协议
  - [x] 定义第一版 SQLite 表结构
  - [x] 打通本地开发启动链路
  - [x] 完成房间聊天 MVP
  - [x] 完成 Agent 作为成员链路
  - [x] 完成助理审批链路
  - [x] 完成本地 Agent 接入基线
  - [x] 完成前端演示壳
  - [x] 收紧 Agent 执行与管理边界
  - [x] 拆分公开 DTO 与内部模型
  - [x] 扩展公开 DTO 覆盖范围
  - [x] 明确 Codex thread 助理接入规则
  - [x] 设计 Codex thread 助理数据模型
  - [x] 完成一次性助理邀请接口基线
  - [x] 初始化 pnpm workspace 工程骨架
  - [x] 初始化 server 应用骨架
  - [x] 初始化 web 应用骨架
  - [x] 初始化 shared 包
  - [x] 初始化 agent-sdk 包
  - [x] 补充 `.gitignore`
  - [x] 完成基础 typecheck
