export function AppLoading() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        minHeight: "100%",
        width: "100%",
        color: "var(--font-tertiary, #64748b)",
        fontSize: 14,
      }}
    >
      Loading...
    </div>
  );
}
